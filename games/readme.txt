Это модуль с файлами, в которых будут функции игр
типа:
    file.py:
    def func(screen):
        # чё-та рисовать
        # по какому-нибудь условию завершать работу функции, например вот так:
        if event.type == pygame.KEYDOWN:
            if event.key == pygame.K_ESCAPE:
                return


предлагаю esc в качестве кнопки выхода в мэин меню

